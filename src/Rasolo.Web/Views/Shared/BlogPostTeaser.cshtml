@model Rasolo.Web.Features.BlogPostPage.BlogPostPage
	<!-- TODO: Refractor to use PostTeaser-->
<article class="posts__post">
	<a class="test" href="@Model.Parent.UrlSegment">
		@{
			if (string.IsNullOrEmpty(Model.TeaserUrl))
			{
				<img src="wwwroot/assets/img/teaser/bear.jpeg" />
			}
			else
			{
				<img src="@Model.TeaserUrl" @if(Model.ShowTeaserMediaAltText){<text>alt="@Model.TeaserMediaAltText"</text>} />
			}
		}
	</a>
	<a class="post__category" href="@Model.Parent.UrlSegment">@Model.Parent.Name</a>
	<a class="post__date" href="@Model.Url">
		<p >@Model.CreateDate.ToString("yyyy-MM-dd")</p>
		<h4>@Model.TeaserHeading</h4>
	</a>

</article>
